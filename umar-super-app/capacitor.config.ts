import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.umar.superapp',
  appName: 'UmarSuperAppndefined',
  webDir: 'public'
};

export default config;
